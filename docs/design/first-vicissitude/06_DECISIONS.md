# 06 — 决策记录与最后确认

第三轮记录，2026-09-12。数值采用首版基准，用户表示伤害等数值无特别意见，后续可调整；不再逐个要求批准冷却/倍率。仍区分用户确定的需求与实施者选取的默认值。

| ID | 结论 |
| --- | --- |
| D1 | first_vicissitude；原稿全部技能、模型、动画、渲染与实际投掷都实现。具体战斗方案见 07。 |
| D2 | 保留四难度三类实际武器。 |
| D3 | bbmodel＋正式贴图＋游戏模型；自主制作，提供最终预览验收。两原型图仅可选参考。 |
| D4 | 环属于身体；翼分段受击、阻挡玩家和地形、共享真生命且有部位倍率。尺寸和倍率采用 01/08 基准。 |
| D5 | 一阶段多球，只有部分追踪，所有球压至最多 1 血；二阶段远程普通伤害。胸/环非球技能按 07 走有预兆的普通伤害。 |
| D6 | 标签免没收已确定。自动返还与右键凭证仍由性能/体验比较决定；推荐自动返还，见 09，尚未将用户的“参考”写成凭证实施要求。 |
| D7 | 数值可后续调整；接入本地 Lodestone 震动能力；新配乐等留扩展，不提前做额外资源。 |
| D8 | FTB 仅举例，不建立专门兼容或“领地＋抗凋灵”组合系统。核心是不得因为不能破坏而卡死：破坏被阻止时用有限搜索的脱困瞬移。具体默认策略见 08。 |
| D9 | 难度越高一阶段越短。最低难度在 150–200 秒内、最高约 60–90 秒；选取 180/140/100/75 秒作首版基准，按 03 保存与迁移。 |
| D10 | 新 item tag `maledict:vicissitude_confiscation_immune`，控制不被没收的 Curio，见 09。 |

## 当前结论

2026-09-13 补充：用户要求继续利用 Lodestone/Malum 其他能力改善特效；已完成本地 API 核对与逐技能接入表（10）。这属于已有视觉范围细化，无需增加新的工具选择审批，不改变 D6 尚未确定的返还交互。

技能伤害、倍率、数量、时序以及模型尺寸可按文档基准实施后调优。转场 60 tick、死亡 80 tick、单次奖励沿用方案。新的时间表替代旧五分钟；不重提 FTB 兼容问题，不把每一个实现常数变成用户问题。

最后一项体验取舍是 D6：**推荐自动返还，不引入强制凭证交互；满背包未交付的饰品保留待返还，在原槽/背包有空间或登录/重生时再次尝试。** 用户目前要求综合比较，未明确选择，所以此项暂记“推荐待定”。凭证是备选，未计入必做物品。

## 冻结条件

- [x] 目标、技能范围、武器、资产交付和可选参考已明确。
- [x] 多球效果、双翼物理碰撞、差异倍率和脱困方向已明确。
- [x] 一阶段难度时长、免没收标签、Lodestone 接入已写入正文。
- [x] 数值允许以首版基准实现并后续调整，无需重复确认每项。
- [ ] D6 推荐自动返还或选择凭证模式后，统一 09 与进度表并标记可实施。

设计材料不是当前实现授权。本次继续完成文档，不修改 Boss 代码。
